services:
  vite-react-app:
    image: ardfordev/react-app:${IMAGE_TAG}
    container_name: react-app
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge